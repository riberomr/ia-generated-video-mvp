{
  "name": "@course-builder/apiv2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/handlers/*.ts --bundle --platform=node --target=node20 --outdir=dist --external:@prisma/client --external:prisma --external:canvas --external:jsdom --external:xmldom",
    "dev": "sam local start-api --warm-containers EAGER --port 3001"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.992.0",
    "@aws-sdk/client-lambda": "^3.992.0",
    "@prisma/client": "^6.0.0",
    "busboy": "^1.6.0",
    "mammoth": "^1.8.0",
    "openai": "^4.0.0",
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/busboy": "^1.5.0",
    "@types/node": "^20.3.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.1.3"
  }
}
